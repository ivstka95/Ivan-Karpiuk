<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ivan.privatnews">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Presenter.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".View.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.CurrencyExchangeActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".View.MainActivity">
        </activity>
        <activity
            android:name=".View.NewsActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".View.MainActivity">
        </activity>
        <activity android:name=".View.ArticleActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".View.NewsActivity"/>

    </application>

</manifest>